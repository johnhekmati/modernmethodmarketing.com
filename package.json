{
  "name": "modernmethodmarketing-com",
  "version": "0.1.0",
  "private": true,
  "description": "Modern Method Marketing — modernmethodmarketing.com",
  "scripts": {
    "preview": "npx --yes serve . -p 5300",
    "deploy": "wrangler pages deploy . --project-name=modern-method-marketing --branch=main --commit-dirty=true"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
